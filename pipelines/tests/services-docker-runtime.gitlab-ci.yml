stages:
  - test

service-docker-runtime:
  stage: test
  image: docker.io/library/alpine:3.19
  services:
    - name: docker.io/library/alpine:3.19
      alias: helper
      command: ["sh", "-lc", "sleep 30"]
      docker:
        platform: linux/arm64/v8
        user: 1000:1000
  script:
    - echo "service docker runtime ok"
