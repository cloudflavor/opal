stages:
  - verify

default:
  image: docker.io/library/alpine:3.19

default-service-aliases:
  stage: verify
  services:
    - library/redis:7.2
  script:
    - nc -z library__redis 6379
    - nc -z library-redis 6379
    - echo "default aliases ok"
