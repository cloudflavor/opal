stages:
  - test

preserved-runtime:
  stage: test
  image: docker.io/library/alpine:3.19
  services:
    - name: docker.io/library/redis:7.2
      alias: cache
      command: ["redis-server"]
  script:
    - nc -z cache 6379
    - echo "runtime preservation ok"
