stages:
  - verify

default:
  image: docker.io/library/alpine:3.19

secret-masking:
  stage: verify
  script:
    - test -n "${API_TOKEN:-}"
    - test -f "${API_TOKEN_FILE:-}"
    - echo "env token=${API_TOKEN}"
    - echo "file token=$(cat "${API_TOKEN_FILE}")"
